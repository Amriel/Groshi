#!/bin/sh
# Перевіряємо саме майбутній коміт; значення секретів не друкуються.
if command -v python >/dev/null 2>&1; then
    exec python scripts/privacy_check.py --staged
fi
exec python3 scripts/privacy_check.py --staged
